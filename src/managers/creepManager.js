module.exports = {
  //Contains logic to run specific creep tasks
    run(room) {
        // Main entrypoint for creepManager.
    },

    loadTasksFromMemory() {
        // Loads tasks, if any, from memory
    }
};
